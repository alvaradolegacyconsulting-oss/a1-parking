# Kickoff — August 9, 2026

**Regroup session that still ships.** Structure per Mateo: don't spend the whole day looking back.

Read [`docs/CURRENT_STATE.md`](CURRENT_STATE.md) first — the SHIPPED and FINDINGS sections summarize where we ended August 8.

---

## The one framing question

🔴 **A1 hardening, or Bar-2 opening?**

Two weeks of A1-focused work has closed most of the surfaces that were rough. Meanwhile Bar-2 has been parked. The next fortnight pulls different ways:

- **Hardening for A1** — swift-handler fix, warnings panel remedies for rows 1/2, CA-portal parity, admin cascade honesty (§6 Commit B), the audit-log/discipline sweeps that surfaced through this arc
- **Opening for strangers** — Bar-2's onboarding flows, self-serve billing scars, help-center completeness, cross-property permission model

Jose has an A1 meeting today ahead of Green Acres on Monday. Worth deciding before those meetings whether the next fortnight is hardening for A1 or opening for strangers — the answer scopes which of the queues below get picked up first.

---

## What today's demo shows A1

**Shipped and verified — no new deploys before the demo.**

- **At-cap V1** (Visitors tab) — plates at their visitor-pass cap with drill-down per plate (time + `visiting_unit`) and the eligible-again date
- **Add Vehicle for existing resident** (PmResidentCrm Vehicles tab) — closes the second-vehicle dead end, with the resident's unit auto-attributed from the detail view
- **Deactivation reasons** — reason codes visible to the next shift on the deactivated panel, entity-scoped
- **No-authorized-vehicle chip** on residents in the CRM list
- **Occupancy flag** on the amber panel
- **Warnings panel** on the manager Insights tab — 6 predicates, plain-language copy, red-above-amber, session-collapsible

Nothing to A1 beyond the demo without an explicit greenlight.

---

## Queued and ready to build

Small enough to ship in a regroup session. Pick from this list based on the A1/Bar-2 framing.

| | Item | Notes |
|---|---|---|
| Warnings §1–4 (V1.1) | ✅ Shipped `7ca0d29` before this doc | Multi-unit duplicate copy, live-only filter, subsumption, red-plate dedup, session-only collapse |
| §6 Commit B — admin cascade | Aggregate-outcome design; column is the gate, bans supplementary | Follows the `a78d129` shape (column-first, three-state severity, `ban_applied: false` audit key) |
| §6 Commit C+ — facilities / reset-pw / property-toggle explicit severity | Sweep against the deprecated `msgBox` sniffer | Same `xActionResult` pattern as `driverActionResult`/`userActionResult` |
| §3 legacy Drivers sub-tab retirement | Only currently-rendered driver-deactivate path in the codebase | Now that `a78d129` verified, can retire the `!CA_CRM_REDESIGN` branch |
| B2 tile mismatch REPORT | Approved permits reads 26 against counts 18/20; moved +3 on one approval | Read-only investigation; no code |

---

## Waiting on Jose

Not builder-side; needs Jose's action.

- **swift-handler body paste** from Supabase dashboard (`deactivate_user` case + user resolution + client construction + env vars). Unblocks the handler fix.
- **Reorder verify at Test Legacy** (`a78d129`) — 5 probes in [commit body](https://github.com/alvaradolegacyconsulting-oss/a1-parking/commit/a78d129).
- **At-cap V1 verify** (`92ededc`) — 9-step recipe in the commit body.
- **`85f9b87` probes 3–5** — collision message quality, gating for pending/declined/deactivated residents, `can_approve_vehicles=false` RPC rejection.
- **Warnings panel probes** — Test Legacy end-to-end (post-Monday).

---

## Designed, not built

Preflight work landed in the FOR MATEO thread but no code:

- **Deactivation emails** — last piece of the Task 3 arc. Templates + resend + `notifyResidentDecision` dedup.
- **Visitor-pass reset V2** — `visitor_pass_resets` table + trigger amendment (`GREATEST(now() - 30d, reset_from)`) + `reset_visitor_pass_counter` DEFINER RPC + `VisitorPassResetReasonCode` entity + exempt-plate tag schema change.
- **Warnings panel CA parity** — either add per-property residents/vehicles fetch to CA, or route both portals through an RPC that embeds `noAuthorizedBucket` + the other 5 predicates. Jose's note: CA portal already has all-properties and per-property selection, so parity is mostly wiring into an existing filter rather than building one.
- **Warnings scroll-to-pending** — expose scroll target from `PmResidentCrm` so the remedy scrolls to the specific resident (V1 just switches tabs).
- **Warnings zero-vehicles-active bucket** — candidate for #4 scope expansion (Jose: expects a lot of hits at Green Acres; going noisy → quiet is harder than the reverse, so held for V2).
- **`/register` step 4 duplicate prevention** — own design pass. #4 in the Aug 8 arc.

---

## Follow-ons filed during the week

- `createUser` rollback copy honesty ("Login account deactivated" asserts unverified rollback — swift-handler fetch is `.catch(() => {})`)
- `/deactivated` page has 3 `hello@shieldmylot.com` + 1 `support@shieldmylot.com` — audit re-run needed against `@shieldmylot.com` class, not just `support@`
- Backlog: vehicles.status/is_active divergence (both directions now observed)
- Backlog: residents-duplicate-row uniqueness (Sayra + others surfaced by the warnings panel)
- Backlog: unit-value normalization (`Apt 117` vs `117` at Green Acres)

---

## Standing constraints

- **Nothing to A1 without an explicit greenlight.**
- **SQL to Jose, not applied by builder.**
- **`public_signup_open` stays false.**
- **No TOS/PRIVACY/SAAS_VERSION bumps.**
- **Test tenants only for verification.**
- **`npx tsc --noEmit` before every push.**
- **Never re-key retired tier strings** (essential/professional/enterprise).
- **Report first for anomalies** — don't fix silently in the same commit as another arc.
